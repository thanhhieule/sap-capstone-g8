 projection;
strict ( 2 );
use draft;

define behavior for ZC_RLDHEAD_G8 //alias <alias_name>
{
  use update;

  use action autoVendor;
  use action convertToPO;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use association _Item { create; with draft; }
}

define behavior for ZC_RLDITEM_G8 //alias <alias_name>
{

  use update;
  use association _Header { with draft; }
}